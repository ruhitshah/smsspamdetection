# Use a Streamlit version that doesn't depend on imghdr
streamlit==1.26.0

# Required to load your saved LogisticRegression model
scikit-learn>=1.0

# Joblib for model persistence
joblib==1.0.1

# Natural Language Toolkit (for stopwords)
nltk==3.6.3

# Pillow provides image-handling functions and is required by Streamlit
pillow==11.3.0

# pin protobuf to avoid compatibility issues 
protobuf==3.20.1

# setuptools; pin lower than 81 to avoid deprecation warnings in Streamlit logs
setuptools>=49.0.0,<81
